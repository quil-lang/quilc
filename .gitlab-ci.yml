test:
  tags:
    - docker
    - python:3.6
  image: rigetti/quicklisp
  script:
    - make dump-version-info
    - make deps
    # clone rpcq to work around QuickLisp
    - pushd /builds/rigetti && git clone https://github.com/rigetti/rpcq.git && popd
    - make test
